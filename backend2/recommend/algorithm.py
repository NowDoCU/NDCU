# from sklearn import preprocessing
# from sklearn.preprocessing import MinMaxScaler
# from sklearn.cluster import KMeans
# import scipy as sp
# import numpy as np

# from .models import Commercial

# def algorithm():

#   print(Commercial.objects.all())
  
#   return

#   # filtered_data = new_data[new_data['상권_구_명'].isin(input_gu)]
#   # filtered_data = filtered_data[filtered_data['서비스_업종_코드_명'].isin(input_category)]

#   # filtered_sg = filtered_data['상권_코드_명']
#   # filtered_data = min_max_scaler.fit_transform(filtered_data[tar_attr])
#   # filtered_data = pd.DataFrame(filtered_data, columns=tar_attr)

#   # filtered_data['상권_코드_명'] = list(filtered_sg)

#   # final_centeroid = rc[0]
#   # tmp = np.array(filtered_data[tar_attr][filtered_data[tar_attr].index == 0])

#   # result_point = sp.spatial.distance.cdist(tmp, rc, "euclidean")